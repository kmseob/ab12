final <- NULL
  
  url='https://gall.dcinside.com/'   
  
  base_url <- paste0(url,sep='')
  
  # 게시글 제목 가져오기
  bbb <- readLines(base_url, encoding = 'UTF-8')
  bbb2 <- bbb[str_detect(bbb,"gallery_view")]
  갤러리id <- str_extract(bbb2, "(?<=gallery_view).*(?=</a>)")
  
  data <- cbind(갤러리id)
  final <- rbind(final, data) 

  head(final)
  tail(final)
  
final